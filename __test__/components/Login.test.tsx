import { render, screen } from '@testing-library/react-native';
import LoginScreen from '@/app/index';

describe('', () => {
    it('', () => {
        // render(<LoginScreen />)
        // expect(screen.getByTestId('login-input-usermail')).toBeTruthy();
    })
})